# QGIS/QFieldローカル同期用 設定ファイル例
# ※このファイルは「UTF-8(BOMなし)」または「Shift_JIS」で保存してください
#   日本語やスペースを含む場合はUTF-8(BOMなし)推奨です

# 例: BOXの環境変数を使いたい場合
# 本体バッチファイル(local-launchar.bat)の先頭で
#   set BOX_ROOT=%USERPROFILE%\Box
# などを記述してください
# configファイル内で set コマンドを書いてもバッチファイル実行時には反映されません

# 同期元フォルダ（日本語・スペース・環境変数も利用可能）
# BOX利用時の例: SYNC_SRC=%BOX_ROOT%\GISシステム
SYNC_SRC=C:\github\yr-qgis-launcher_new

# 同期先フォルダ（日本語・スペース・環境変数も利用可能）
# SYNC_DST=%BOX_ROOT%\qgis-local-launcher
SYNC_DST=C:\qgis-local-launcher

# QFieldバージョン（例: 3.4.2 など。日付や任意文字列でも可）
QFIELD_VERSION=3.4.2

# QGISバージョン（例: 3.42.0 など。日付や任意文字列でも可）
QGIS_VERSION=3.42.0

# 同期したくないフォルダ名（スペース区切り、日本語・スペースも可、例: シークレット secret-folder private-data）
EXCLUDE_DIRS=secret-folder private-data シークレット
